# CLAUDE Is Canonical

Treat `CLAUDE.md` at the repository root as canonical. Public engine docs are mirrored here from `blit386/docs/`; the
canonical edit location stays the sibling `blit386` repo. See `CLAUDE.md` (Documentation mirror) and
`DOCUMENTATION_MIGRATION.md`.
