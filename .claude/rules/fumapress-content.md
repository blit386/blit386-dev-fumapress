# Fumapress Content

MDX frontmatter required (`title`, `description`). Public engine docs are mirrored here from `blit386/docs/`: edit the
canonical copy in the engine repo first, then sync `content/docs/**/index.mdx`. In ported pages, intra-doc links use
root-relative site paths (`/docs/...`); contributor-only pages stay link-only on GitHub. No emoji.
