---
title: Hello World
description: First BLIT386 blog post — a placeholder for testing the blog collection.
tags: [announcement]
---

This is a placeholder blog post created to verify that the blog content collection, `blogPlugin`, and the multi-source
`content` config are all wired up correctly.

## What this tests

- `content/blog/**` is owned by the `blog` collection (type `'blog'`), not `docs`
- The `blogPlugin` renders an index at `/blog` and a per-post layout
- The `docs` collection explicit-include list excludes `content/blog/**`

Delete this file once a real post exists.
